<Window x:Class="CSVReaderTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CSVReaderTool"
        xmlns:Converter="clr-namespace:CSVReaderTool.Logik"
        mc:Ignorable="d"
        Title="CSVReader" MinHeight="500" MinWidth="700" Height="650" Width="1000">
    <Window.Resources>
        <Converter:HttpColor x:Key="HttpColor"/>

        <DataTemplate x:Key="HeaderCheckBoxTemplate">
            <CheckBox Content="{Binding Name}"
              IsChecked="{Binding IsChecked}"
              VerticalAlignment="Center"/>
        </DataTemplate>
    </Window.Resources>
    
    <Grid Background="LightGray">
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition />
            <RowDefinition Height="70"/>
        </Grid.RowDefinitions>

        <DataGrid Grid.Row="1" Margin="35,0,35,30" ItemsSource="{Binding TableView}" AutoGenerateColumns="True" AutoGeneratingColumn="dataGrid_AutoGeneratingColumn"  LoadingRow="DataGrid_LoadingRow"/>

        <Grid Background="DarkGray" Grid.Row="2" >

            <Grid.RowDefinitions>
                <RowDefinition Height="1"/>
                <RowDefinition />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition />
                <ColumnDefinition Width="auto" MaxWidth="160"/>
                <ColumnDefinition Width="auto" MaxWidth="160"/>

            </Grid.ColumnDefinitions>

            <Rectangle Fill="Gray" Grid.ColumnSpan="4"/>
            <Button Margin="10" Grid.Row="1" Width="140" Content="Datei auswählen" Command="{Binding DateiAuswaehlenCommand}"/>
            <Label Grid.Column="1" Grid.Row="1" Margin="0,20,40,18" Content="{Binding DateiName}" Width="auto" FontSize="15" Background="White" BorderBrush="Gray" BorderThickness="1"/>
            <Button Grid.Column="2" Grid.Row="1" Margin="10" Width="140" Content="Einlesen" Command="{Binding DateiAuslesenCommand}" />
            <Button Grid.Column="3" Grid.Row="1" Margin="10" Width="140" Content="Exportieren" IsEnabled="{Binding Enabled}" Command="{Binding DateiExportCommand}"/>

        </Grid>
        <Grid Grid.RowSpan="3" Visibility="{Binding Loading}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition />
            </Grid.RowDefinitions>

            <Rectangle Fill="Gray" Opacity="0.5" Grid.ColumnSpan="3" Grid.RowSpan="4"/>

            <StackPanel Orientation="Horizontal" Grid.Column="1" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel.Triggers>
                    <EventTrigger RoutedEvent="Rectangle.Loaded">
                        <BeginStoryboard>
                            <Storyboard Name="waitingAnimation" RepeatBehavior="Forever">
                                <DoubleAnimation Storyboard.TargetName="Dot1" BeginTime="0:0:0.1" Storyboard.TargetProperty="Opacity" From=".1" To="1" AutoReverse="True" Duration="0:0:0.4"/>
                                <DoubleAnimation Storyboard.TargetName="Dot2" BeginTime="0:0:0.2" Storyboard.TargetProperty="Opacity" From=".1" To="1" AutoReverse="True" Duration="0:0:0.4"/>
                                <DoubleAnimation Storyboard.TargetName="Dot3" BeginTime="0:0:0.3" Storyboard.TargetProperty="Opacity" From=".1" To="1" AutoReverse="True" Duration="0:0:0.4"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </StackPanel.Triggers>
                <Ellipse Name="Dot1" Fill="Black" Opacity=".1" Width="15" Height="15"/>
                <Ellipse Name="Dot2" Fill="Black" Opacity=".1" Width="15" Height="15" Margin="4,0,0,0"/>
                <Ellipse Name="Dot3" Fill="Black" Opacity=".1" Width="15" Height="15" Margin="4,0,0,0"/>
            </StackPanel>

            <Button Content="Abbrechen" Grid.Column="1" Grid.Row="2" Margin="10,25,10,10" MinHeight="40" MinWidth="100" Command="{Binding AbbrechenCommand}"/>
        </Grid>
        
    </Grid>
</Window>
